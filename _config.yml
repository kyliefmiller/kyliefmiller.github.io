title: Kylie Miller
author:
  name: Kylie Miller
  email: kylie.f.miller@ucdenver.edu

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >
  This is my portfolio page for the Working with Data class!
url: "https://kyliefmiller.github.io" # the base hostname & protocol for your site, e.g. http://example.com
baseurl: ""

# Build settings

# If you clone the Minima repo and build locally, use this setting.
theme: minima

# As of November 2023, GitHub Pages still uses Minima 2.5.1 (https://pages.github.com/versions/).
# If you want to use the latest Minima version on GitHub Pages, use the following setting and
# add a line comment on "theme: minima" above.
#remote_theme: jekyll/minima

# Site Collections (like posts but without pesky date/time requirements)
collections:
  notebooks:
    output: true

plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Navigation Links
navigation:
  main:
    - title: "About Me"
      url: "/"
    - title: "Portfolio"
      url: "/portfolio/"
    - title: "Project Review"
      url: "/project-review/"
    - title: "Skills"
      url: "/skills/"

include: ["_pages"]
permalink: /:title/

# Minima specific settings, which are only available from Minima 3.0 onward.
minima:
  # Minima skin selection.
  # Available skins are:
  # classic	           Default, light color scheme.
  # dark	             Dark variant of the classic skin.
  # auto	             Adaptive skin based on the default classic and dark skins.
  # solarized-light	   Light variant of solarized color scheme.
  # solarized-dark	   Dark variant of solarized color scheme.
  # solarized	         Adaptive skin for solarized color scheme skins.
  skin: classic

  # Set to `true` to show excerpts on the homepage.
  #show_excerpts: false

  # Minima date format.
  # The default value is "%b %d, %Y" (e.g. Nov 14, 2023)
  # Refer to https://shopify.github.io/liquid/filters/date/ if you want to customize this.
  #date_format: "%b-%d-%Y"

  # Social Media Links
  # Renders icons via Font Awesome Free webfonts CDN, based on ordered list of entries.
  #
  # Entry keys:
  # * title    Tooltip rendered on hovering over icon.
  # * icon     Font Awesome icon id. `github` corresponds to `fa-github`.
  # * url      Full URL of social profile.
  social_links:
    - title: Github
      icon: github
      url: "https://github.com/kyliefmiller"
    - title: Linkedin
      icon: linkedin
      url: "https://www.linkedin.com/in/kylie-miller-564156128/"
    - title: Instagram (Photography Page)
      icon: instagram
      url: "https://www.instagram.com/kyfotos/"
  #  - title: Jekyll at X (formerly Twitter)
  #    icon: x-twitter
  #    url: "https://x.com/jekyllrb"
